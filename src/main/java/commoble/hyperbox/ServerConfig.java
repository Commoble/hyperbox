package commoble.hyperbox;

import net.minecraftforge.common.ForgeConfigSpec;

public class ServerConfig
{
//	public final ConfigObjectListener<ResourceLocation> biome;
//	public final ConfigObjectListener<Integer> height;
	
	public ServerConfig(ForgeConfigSpec.Builder builder, ConfigHelper.Subscriber subscriber)
	{
//		builder.push("dimension");
//		builder.comment("ID of the biome that newly generated hyperbox dimensions will use. Will not affect existing boxes.");
//		subscriber.subscribeObject(builder, "biome", ResourceLocation.CODEC, Hyperbox.HYPERBOX_ID);
//		builder.comment("Height of the hyperbox dimension. The ceiling of the box will be at the level one block below this.",
//			"Only affects newly generated hyperbox dimensions, not existing ones.");
//		subscriber.subscribeObject(builder, "height", Codec.INT, 16);
//		builder.pop();
	}
}
